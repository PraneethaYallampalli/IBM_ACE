

CREATE COMPUTE MODULE putId_Compute
	CREATE FUNCTION Main() RETURNS BOOLEAN
	BEGIN
		CALL CopyMessageHeaders();
		-- CALL CopyEntireMessage();
		DECLARE inref REFERENCE TO InputRoot.JSON.Data;
        DECLARE id CHARACTER InputLocalEnvironment.REST.Input.Parameters.id;
        DECLARE accountNumber CHARACTER;
        

--       UPDATE Database.ibm_ace.bank_detailss 
--       SET Environment.ibm_ace.bank_detailss.accountHolderName = inref.accountHolderName
--       WHERE accountNumber = inref.accountNumber;
--
--          SET OutputRoot.JSON.Data.id = id;
--          SET OutputRoot.JSON.Data.accountNumber = Environment.ibm_ace.bank_detailss.accountNumber;
--          SET OutputRoot.JSON.Data.accountHolderName = Environment.ibm_ace.bank_detailss.accountHolderName;
--		

  
		RETURN TRUE;
	END;

	CREATE PROCEDURE CopyMessageHeaders() BEGIN
		DECLARE I INTEGER 1;
		DECLARE J INTEGER;
		SET J = CARDINALITY(InputRoot.*[]);
		WHILE I < J DO
			SET OutputRoot.*[I] = InputRoot.*[I];
			SET I = I + 1;
		END WHILE;
	END;

	CREATE PROCEDURE CopyEntireMessage() BEGIN
		SET OutputRoot = InputRoot;
	END;
END MODULE;
